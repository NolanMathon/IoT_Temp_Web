<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="css/style.css" rel="stylesheet" type="text/css">
    <title>Document</title>
</head>
<body>
    <header class="navbar">Centre d'opérations IoT</header>

    <div class="grid">
        <% capteurs.forEach(function(capteur){%>
        <div class="item">
            <center><h3><%= capteur.nom %></h3></center><br>
            <p>Température : <%= capteur.temp %>°C</p>
            <p>Min : <%= capteur.minTemp %>°C</p>
            <p>Max : <%= capteur.maxTemp %>°C</p>
        </div>
        <% }); %>
    </div>

    <% var temp = capteurs[0].temp %>

    <script>
        var getTemp = '<%= temp %>';
        // var card = document.getElementsById('item');

        if(getTemp < 18)
        {
        }
        console.log(getTemp);   
    </script>
</body>
</html>